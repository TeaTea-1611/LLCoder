query Blogs($limit: Int!, $cursor: String) {
  blogs(limit: $limit, cursor: $cursor) {
    totalCount
    cursor
    hashMore
    blogs {
      id
      title
      markdown
      createdAt
      updatedAt
    }
  }
}
